import LoggerItem from "./components/LoggerItem/LoggerItem";
import "./logger.scss";

const Logger = () => {
   return (
      <div>
         <LoggerItem info={"test"} />
      </div>
   );
};

export default Logger;
